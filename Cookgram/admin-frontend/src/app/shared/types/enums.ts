export enum Role {
  User = 'User',
  Admin = 'Admin',
  SuperAdmin = 'SuperAdmin',
  Employee = 'Employee',
  Manager = 'Manager',
  Director = 'Director',
}

export enum Gender {
  Man = 'Man',
  Woman = 'Woman',
}

export enum ObjectState {
  Draft = 'Draft',
  Active = 'Active',
  Suspend = 'Suspend',
  Frozen = 'Frozen',
  Retired = 'Retired',
  Deleted = 'Deleted',
}
