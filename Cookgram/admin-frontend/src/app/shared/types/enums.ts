export enum Role {
  User = 'User',
  Admin = 'Admin',
  SuperAdmin = 'SuperAdmin',
  Employee = 'Employee',
  Manager = 'Manager',
  Director = 'Director',
}

export enum Gender {
  Man = 'Man',
  Woman = 'Woman',
  None = 'None',
}

export enum State {
  Draft = 'Draft',
  Active = 'Active',
  Suspend = 'Suspend',
  Frozen = 'Frozen',
  Retired = 'Retired',
  Deleted = 'Deleted',
}
export enum SessionItemName {
  Token = 'token',
  Tokens = 'tokens',
  AccessToken = 'accessToken',
  RefreshToken = 'refreshToken',
}
